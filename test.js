/*(async () => {
    while (true) {
        for (const text of document.getElementsByClassName("blocklyText")) {
            text.style.fontFamily = "Helvetica Neue, Helvetica, Arial, sans-serif"
        }
        await new Promise(resolve => setTimeout(resolve, 100))
    }
})()*/